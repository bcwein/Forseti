"""Test Function."""


def add_one(number):
    """add_one

    Args:
        number (number): Number that we want to increment with 1.

    Returns:
        number: number + 1
    """
    return number + 1

